
  <div class="card card-blog card-atv">
  	<div class="card-image">
  		<a href="#pablo">
  			<div class="atvImg">
          <%= image_tag @post.picture %>
  			</div>
  		</a>
  	</div>

  	<div class="card-content">
  		<h4 class="card-title">
  			<a href="#pablo">This is a perspective card, the image is moving on hover</a>
  		</h4>
  		<p class="card-description">
  			Don't be scared of the truth because we need to restart the human foundation in truth And I love you like Kanye loves Kanye I love Rick Owens’ bed design but the back is...
  		</p>
  		<div class="footer">
  			<div class="author">
  				<a href="#pablo">
  				   <img src="assets/img/faces/marc.jpg" alt="..." class="avatar img-raised">
  				   <span>Mike John</span>
  				</a>
  			</div>
  		   <div class="stats">
  				<i class="material-icons">schedule</i> 5 min read
  			</div>
  		</div>
  	</div>
  </div>

<!-- <div class="pull-right">
  <% if post_deletable?(@post, current_user) %>
  <%= link_to post_path(@post), method: :delete, class:'btn btn-danger', data: {confirm: '确定要删除照片吗?'} do %>
  <i class="fa fa-trash-o" aria-hidden="true"></i>
  <% end %>
  <% end %>
</div> -->
<!-- <%= render 'post_info', post: @post %>
<%= image_tag @post.picture %><br> -->
发图人的头像和名称
<%= render 'user_info', user: @post.user %><br>

<%# TODO: 图片地点  %>
<%= render 'post_location' %>
<br>
<%= render 'like_button', post: @post%>
<br>

<%= @post.likers.count %> 人喜欢
喜欢的人除了自己的头像
头像 名字     时间
内容

<hr>

<i class="fa fa-comment-o" aria-hidden="true"><%= @post.comments.count %>条评论</i>
<%= simple_form_for @comment, url: post_comments_path(@post), method: :post do |f| %>
    <div class="group">
      <%= f.input :content %>
    </div>

    <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>
<% end %>

<div class="">
  <% @comments.each do |comment| %>
  <%= render 'post_comment', comment: comment %>
  <% end %>
</div>
